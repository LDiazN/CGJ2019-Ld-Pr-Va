using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraControllerAdventure : MonoBehaviour
{
    //This gameObject:

    public static CameraControllerAdventure gameCam;

    // Start is called before the first frame update
    void Start()
    {
        gameCam = this;    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
