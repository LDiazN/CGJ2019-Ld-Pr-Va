using UnityEngine;
using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;

public class TextFileReader : MonoBehaviour
{
    //public DialogsList DialogList = new DialogsList();
    //
    //public void ReadFile(string sceneName)
    //{
    //    TextAsset asset = Resources.Load(sceneName) as TextAsset;
    //    if (asset != null)
    //    {
    //        DialogList = JsonUtility.FromJson<DialogsList>(asset.text);
    //        foreach (Scene_Dialogs dialog in DialogList.Scene_Dialogs)
    //        {
    //            print(dialog.dialog_text);
    //            print(dialog.character);
    //        }
    //    }
    //}   
}
